<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="headlogo.png" type="image/png">
    <title>Project Developments</title>
    <!-- Google Fonts and Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Space+Grotesk:wght@400;500;600;700&display=swap');

        :root {
            --primary: #6366f1;
            --primary-dark: #4f46e5;
            --secondary: #10b981;
            --dark: #0f172a;
            --light: #f8fafc;
            --gray: #64748b;
            --light-gray: #e2e8f0;
            --card-bg: rgba(255, 255, 255, 0.85);
            --glass-effect: rgba(255, 255, 255, 0.7);
            --glass-border: rgba(255, 255, 255, 0.3);
            --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-md: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            --transition: all 0.25s ease-out;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #f1f5f9;
            color: var(--dark);
            overflow-x: hidden;
            font-family: 'Inter', sans-serif;
            position: relative;
            line-height: 1.6;
        }

        /* Simplified background with subtle gradient */
        .background-wrapper {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            overflow: hidden;
        }

        .gradient-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 70% 30%, rgba(99, 102, 241, 0.08) 0%, transparent 60%),
                radial-gradient(circle at 30% 70%, rgba(16, 185, 129, 0.08) 0%, transparent 60%);
        }

        /* Glassmorphism effect */
        .glass-card {
            background: var(--glass-effect);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            border-radius: 16px;
            border: 1px solid var(--glass-border);
            box-shadow: var(--shadow);
            transition: var(--transition);
        }

        /* Projects Hero Section */
        .projects-hero {
            min-height: 60vh;
            display: flex;
            align-items: center;
            padding: 10rem 5% 5rem;
            position: relative;
        }

        .projects-hero-content {
            max-width: 900px;
            margin: 0 auto;
            position: relative;
            z-index: 2;
            text-align: center;
        }

        .projects-hero-title {
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 800;
            margin-bottom: 1.5rem;
            color: var(--dark);
            font-family: 'Space Grotesk', sans-serif;
            line-height: 1.1;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            display: inline-block;
        }

        .projects-hero-subtitle {
            font-size: clamp(1rem, 2vw, 1.25rem);
            color: var(--gray);
            line-height: 1.8;
            margin-bottom: 2.5rem;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }

        /* Projects Content Section */
        .projects-section {
            padding: 3rem 5% 6rem;
            position: relative;
        }

        .projects-container {
            max-width: 1200px;
            margin: 0 auto;
        }

        /* Projects Grid */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 2.5rem;
            margin-bottom: 4rem;
        }

        .project-card {
            padding: 2rem;
            border-radius: 16px;
            background: var(--card-bg);
            box-shadow: var(--shadow);
            transition: var(--transition);
            display: flex;
            flex-direction: column;
            height: 100%;
        }

        .project-card:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow-md);
        }

        .project-image {
            width: 100%;
            border-radius: 12px;
            overflow: hidden;
            margin-bottom: 1.5rem;
        }

        .project-image img {
            width: 100%;
            height: auto;
            display: block;
            transition: var(--transition);
            border-radius: 12px;
        }

        .project-card:hover .project-image img {
            transform: scale(1.03);
        }

        .project-title {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: var(--dark);
            font-family: 'Space Grotesk', sans-serif;
            line-height: 1.2;
        }

        .project-description {
            font-size: 1rem;
            line-height: 1.7;
            color: var(--gray);
            margin-bottom: 1.5rem;
            flex-grow: 1;
        }

        .project-tech {
            display: flex;
            flex-wrap: wrap;
            gap: 0.8rem;
            margin-bottom: 1.5rem;
        }

        .tech-tag {
            padding: 0.4rem 1rem;
            background: rgba(99, 102, 241, 0.1);
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 500;
            color: var(--primary);
            transition: var(--transition);
            border: 1px solid rgba(99, 102, 241, 0.2);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .tech-icon {
            font-size: 1.1rem;
        }

        .project-card:hover .tech-tag {
            background: rgba(99, 102, 241, 0.2);
            transform: translateY(-2px);
        }

        /* Buttons */
        .btn {
            padding: 0.9rem 2rem;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            font-family: 'Inter', sans-serif;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            text-decoration: none;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            box-shadow: var(--shadow);
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, var(--primary-dark), var(--primary));
            box-shadow: var(--shadow-md);
            transform: translateY(-2px);
        }

        .btn-outline {
            background: transparent;
            border: 1px solid var(--primary);
            color: var(--primary);
        }

        .btn-outline:hover {
            background: rgba(99, 102, 241, 0.1);
            transform: translateY(-2px);
        }

        /* Contact Section */
        .contact-section {
            margin-top: 6rem;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2rem;
            align-items: start;
            justify-items: center;
        }

        .email-form-container {
            padding: 2.5rem;
            border-radius: 16px;
            box-shadow: var(--shadow);
            border: 1px solid var(--glass-border);
            height: 100%;
            width: 100%;
        }

        .email-form-title {
            font-size: 1.75rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: var(--dark);
            font-family: 'Space Grotesk', sans-serif;
        }

        .email-form-description {
            color: var(--gray);
            margin-bottom: 2rem;
            font-size: 1rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
            text-align: left;
        }

        .form-label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: var(--dark);
        }

        .form-input {
            width: 100%;
            padding: 0.9rem 1.2rem;
            border: 1px solid var(--light-gray);
            border-radius: 8px;
            font-size: 1rem;
            transition: var(--transition);
            background-color: white;
        }

        .form-input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2);
        }

        .form-textarea {
            min-height: 120px;
            resize: vertical;
        }

        .submit-btn {
            width: 100%;
            padding: 1rem;
            font-size: 1rem;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: var(--transition);
            font-weight: 600;
        }

        .submit-btn:hover {
            background: linear-gradient(135deg, var(--primary-dark), var(--primary));
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

        /* FAQ Section */
        .faq-container {
            padding: 2.5rem;
            border-radius: 16px;
            box-shadow: var(--shadow);
            border: 1px solid var(--glass-border);
            height: 100%;
            width: 100%;
        }

        .faq-title {
            font-size: 1.75rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: var(--dark);
            font-family: 'Space Grotesk', sans-serif;
        }

        .faq-item {
            margin-bottom: 1.5rem;
            border-radius: 8px;
            overflow: hidden;
            transition: var(--transition);
        }

        .faq-question {
            background: rgba(99, 102, 241, 0.1);
            padding: 1rem 1.5rem;
            font-weight: 600;
            color: var(--dark);
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: var(--transition);
        }

        .faq-question:hover {
            background: rgba(99, 102, 241, 0.2);
        }

        .faq-question i {
            transition: var(--transition);
        }

        .faq-answer {
            padding: 0 1.5rem;
            max-height: 0;
            overflow: hidden;
            transition: var(--transition);
            color: var(--gray);
            background: rgba(241, 245, 249, 0.7);
        }

        .faq-item.active .faq-question {
            background: rgba(99, 102, 241, 0.2);
        }

        .faq-item.active .faq-question i {
            transform: rotate(180deg);
        }

        .faq-item.active .faq-answer {
            padding: 1rem 1.5rem;
            max-height: 300px;
        }

        /* Success Message */
        .success-message {
            background: rgba(16, 185, 129, 0.15);
            border: 1px solid var(--secondary);
            color: var(--secondary);
            padding: 1rem;
            border-radius: 8px;
            margin-top: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-weight: 500;
            animation: fadeIn 0.5s ease;
        }

        .success-message i {
            font-size: 1.5rem;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Success Modal */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            opacity: 0;
            visibility: hidden;
            transition: var(--transition);
        }

        .modal-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .success-modal {
            background: white;
            border-radius: 16px;
            padding: 2.5rem;
            text-align: center;
            max-width: 450px;
            width: 90%;
            box-shadow: var(--shadow-lg);
            transform: translateY(20px);
            transition: var(--transition);
            position: relative;
        }

        .modal-overlay.active .success-modal {
            transform: translateY(0);
        }

        .success-modal-icon {
            font-size: 3.5rem;
            color: var(--secondary);
            margin-bottom: 1.5rem;
        }

        .success-modal-title {
            font-size: 1.75rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: var(--dark);
            font-family: 'Space Grotesk', sans-serif;
        }

        .success-modal-text {
            color: var(--gray);
            margin-bottom: 2rem;
            font-size: 1.05rem;
            line-height: 1.6;
        }

        .success-modal-btn {
            padding: 0.9rem 2.5rem;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            font-size: 1rem;
            box-shadow: var(--shadow);
        }

        .success-modal-btn:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

        /* Responsive adjustments */
        @media (max-width: 1200px) {
            .projects-grid {
                grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            }
        }

        @media (max-width: 992px) {
            .contact-section {
                grid-template-columns: 1fr;
            }
            
            .email-form-container, .faq-container {
                width: 100%;
                max-width: 600px;
                margin: 0 auto;
            }
        }

        @media (max-width: 768px) {
            .projects-hero {
                padding: 6rem 5% 3rem;
            }

            .projects-hero-title {
                font-size: 2.2rem;
            }

            .email-form-container, .faq-container {
                padding: 1.5rem;
            }
            
            .success-modal {
                padding: 2rem 1.5rem;
            }
        }

        @media (max-width: 576px) {
            .projects-grid {
                grid-template-columns: 1fr;
            }

            .success-modal-title {
                font-size: 1.5rem;
            }
            
            .success-modal-text {
                font-size: 1rem;
            }
        }
    </style>
</head>

<body>
    <div class="background-wrapper">
        <div class="gradient-bg"></div>
    </div>

    <!-- navbar.ejs -->
    <%- include('navbar') %>

    <!-- Projects Hero Section -->
    <section class="projects-hero">
        <div class="projects-hero-content">
            <h1 class="projects-hero-title">My Projects</h1>
            <p class="projects-hero-subtitle">
                A showcase of my recent work and development projects
            </p>
        </div>
    </section>

    <!-- Projects Content Section -->
    <section class="projects-section">
        <div class="projects-container">
            <div class="projects-grid">
                <!-- Project 1 -->
                <div class="project-card glass-card">
                    <div class="project-image">
                        <img src="/api/placeholder/600/400" alt="Task Management App" loading="lazy">
                    </div>
                    <h3 class="project-title">Task Management App</h3>
                    <p class="project-description">Productivity app with drag-and-drop tasks, reminders, and team collaboration features.</p>
                    <div class="project-tech">
                        <span class="tech-tag"><i class="fab fa-html5 tech-icon"></i> HTML5</span>
                        <span class="tech-tag"><i class="fas fa-wind tech-icon"></i> Tailwind</span>
                        <span class="tech-tag"><i class="fas fa-database tech-icon"></i> MongoDB</span>
                    </div>
                    <a href="#" class="btn btn-outline">
                        <i class="fas fa-eye"></i> View Details
                    </a>
                </div>

                <!-- Project 2 -->
                <div class="project-card glass-card">
                    <div class="project-image">
                        <img src="/api/placeholder/600/400" alt="Portfolio Website" loading="lazy">
                    </div>
                    <h3 class="project-title">Portfolio Website</h3>
                    <p class="project-description">Responsive personal portfolio with smooth animations showcasing skills and projects.</p>
                    <div class="project-tech">
                        <span class="tech-tag"><i class="fab fa-html5 tech-icon"></i> HTML5</span>
                        <span class="tech-tag"><i class="fab fa-css3-alt tech-icon"></i> CSS3</span>
                        <span class="tech-tag"><i class="fab fa-js tech-icon"></i> JavaScript</span>
                    </div>
                    <a href="#" class="btn btn-outline">
                        <i class="fas fa-eye"></i> View Details
                    </a>
                </div>

                <!-- Project 3 -->
                <div class="project-card glass-card">
                    <div class="project-image">
                        <img src="/api/placeholder/600/400" alt="Blog Platform" loading="lazy">
                    </div>
                    <h3 class="project-title">Blog Platform</h3>
                    <p class="project-description">CMS for bloggers with markdown support, authentication, and comments.</p>
                    <div class="project-tech">
                        <span class="tech-tag"><i class="fab fa-python tech-icon"></i> Django</span>
                        <span class="tech-tag"><i class="fab fa-python tech-icon"></i> Python</span>
                        <span class="tech-tag"><i class="fab fa-bootstrap tech-icon"></i> Bootstrap</span>
                    </div>
                    <a href="#" class="btn btn-outline">
                        <i class="fas fa-eye"></i> View Details
                    </a>
                </div>
            </div>

            <!-- Contact Section with Form and FAQ -->
            <div class="contact-section">
                <div class="email-form-container glass-card">
                    <h3 class="email-form-title">Have a question about my work?</h3>
                    <p class="email-form-description">Feel free to reach out with any inquiries or project ideas.</p>

                    <form id="projectInquiryForm" action="/projectquery" method="post">
                        <div class="form-group">
                            <label for="email" class="form-label">Your Email</label>
                            <input type="email" id="email" name="email" class="form-input"
                                placeholder="Enter your email" required>
                        </div>
                        <div class="form-group">
                            <label for="message" class="form-label">Your Message</label>
                            <textarea id="message" name="message" class="form-input form-textarea"
                                placeholder="Tell me about your project or question" required></textarea>
                        </div>
                        <button type="submit" class="submit-btn">Send Message</button>
                    </form>
                    <!-- Success Message (fallback) -->
                    <div id="formSuccessMessage" class="success-message" style="display: none;">
                        <i class="fas fa-check-circle"></i>
                        <span>Thank you! Your message has been sent successfully.</span>
                    </div>
                </div>

                <!-- FAQ Section -->
                <div class="faq-container glass-card">
                    <h3 class="faq-title">Frequently Asked Questions</h3>
                    <div class="faq-item">
                        <div class="faq-question">
                            <span>What technologies do you specialize in?</span>
                            <i class="fas fa-chevron-down"></i>
                        </div>
                        <div class="faq-answer">
                            <p>I specialize in modern web technologies including HTML5, CSS3, JavaScript, Node.js, and various databases. I also have experience with Python frameworks like Django.</p>
                        </div>
                    </div>
                    
                    <div class="faq-item">
                        <div class="faq-question">
                            <span>How long does a typical project take?</span>
                            <i class="fas fa-chevron-down"></i>
                        </div>
                        <div class="faq-answer">
                            <p>Project timelines vary based on complexity. A simple website might take 2-4 weeks, while more complex applications can take 2-6 months.</p>
                        </div>
                    </div>
                    
                    <div class="faq-item">
                        <div class="faq-question">
                            <span>What's your development process?</span>
                            <i class="fas fa-chevron-down"></i>
                        </div>
                        <div class="faq-answer">
                            <p>I follow an agile approach with clear milestones: planning, design, development, testing, and deployment. Regular updates ensure the project stays aligned with your vision.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- footer.ejs -->
    <%- include('footer') %>

    <!-- Success Modal -->
    <div class="modal-overlay" id="successModal">
        <div class="success-modal">
            <div class="success-modal-icon">
                <i class="fas fa-check-circle"></i>
            </div>
            <h3 class="success-modal-title">Message Sent !</h3>
            <p class="success-modal-text">Thank you for reaching out. I'll get back to you as soon as possible.</p>
            <button class="success-modal-btn" id="closeModalBtn">Get it!</button>
        </div>
    </div>

    <script>
        // Form submission
        document.getElementById('projectInquiryForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const form = this;
            const submitBtn = form.querySelector('.submit-btn');
            const successModal = document.getElementById('successModal');
            
            // UI Feedback
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Sending...';
            
            try {
                const response = await fetch(form.action, {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({
                        email: form.email.value,
                        message: form.message.value
                    })
                });

                if (!response.ok) throw new Error(await response.text());
                
                // Show success modal
                form.reset();
                successModal.classList.add('active');
            } catch (error) {
                console.error('Submission error:', error);
                const successMessage = document.getElementById('formSuccessMessage');
                showMessage(successMessage,
                           `<i class="fas fa-exclamation-circle"></i> ${error.message || 'Submission failed'}`,
                           'error');
            } finally {
                submitBtn.disabled = false;
                submitBtn.textContent = 'Send Message';
            }
        });

        // Close modal handler
        document.getElementById('closeModalBtn').addEventListener('click', function() {
            document.getElementById('successModal').classList.remove('active');
        });

        // Close modal when clicking outside
        document.getElementById('successModal').addEventListener('click', function(e) {
            if (e.target === this) {
                this.classList.remove('active');
            }
        });

        // FAQ Accordion
        document.querySelectorAll('.faq-question').forEach(question => {
            question.addEventListener('click', () => {
                const item = question.parentElement;
                item.classList.toggle('active');
                
                // Close other open items
                document.querySelectorAll('.faq-item').forEach(otherItem => {
                    if (otherItem !== item && otherItem.classList.contains('active')) {
                        otherItem.classList.remove('active');
                    }
                });
            });
        });

        function showMessage(element, html, type) {
            element.innerHTML = html;
            element.style.display = 'flex';
            element.style.background = type === 'success' 
                ? 'rgba(16, 185, 129, 0.15)' 
                : 'rgba(239, 68, 68, 0.15)';
            element.style.borderColor = type === 'success' ? '#10b981' : '#ef4444';
            element.style.color = type === 'success' ? '#10b981' : '#ef4444';
            
            setTimeout(() => element.style.display = 'none', 5000);
        }
    </script>
</body>
</html>